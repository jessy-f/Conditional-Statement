using System;

class Program
{
    static void Main()
    {
        int number = 16;
        if (number % 2 == 0)
        {
            Console.WriteLine("The number {0} is even.", number);
        }
    }
}
