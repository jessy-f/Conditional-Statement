using System;

class Program
{
    static void Main()
    {
        int number = 8;
        if (number > 0)
        {
            Console.WriteLine("The number {0} is positive.", number);
        }
        else
        {
            Console.WriteLine("The number {0} is negative.", number);
        }
    }
}
